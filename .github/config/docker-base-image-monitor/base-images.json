{
  "$schema": "https://raw.githubusercontent.com/bauer-group/automation-templates/main/.github/config/docker-base-image-monitor/docker-base-images.schema.json",
  "_description": "MTA-STS stack — the application image is built FROM python:3.14-alpine. The monitor tracks that base image's digest and triggers a release (rebuild) when it moves.",
  "images": [
    {
      "name": "python-alpine",
      "image": "python",
      "tag": "3.14-alpine",
      "variable": "PYTHON_ALPINE_DIGEST",
      "description": "Base image for the MTA-STS application container (src/Dockerfile)"
    }
  ],
  "settings": {
    "commit-prefix": "chore(deps)",
    "auto-create-variables": true
  }
}
