# Test dependencies for the MTA-STS Server.
# Installed in the Dockerfile "test" stage (the build gate) and usable locally:
#   pip install -r requirements-test.txt && pytest
-r requirements.txt

pytest>=9.1.0,<10.0.0
pytest-mock>=3.15.1,<4.0.0
