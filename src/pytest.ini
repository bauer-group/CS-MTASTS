[pytest]
testpaths = tests
# app.py lives at the context root (not a package); add the rootdir to sys.path
# so `import app` resolves both locally and inside the Docker test stage.
pythonpath = .
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning
